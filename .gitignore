*.iml
.gradle/
build/
# Local, machine-specific SDK path — never commit an absolute SDK path.
local.properties
.idea/*
!.idea/runConfigurations/
.DS_Store
captures/
.externalNativeBuild/
.cxx/
*.apk
*.aab

# Play Store signing key material — must never enter the repo (see
# docs/release/play-store.md section B). No such file exists yet; this is a
# guard rail for whoever runs `keytool` next.
*.jks
*.keystore
keystore.properties
certificates/
.kotlin/

# WSL/NTFS metadata artifact left behind when a file is copied from Windows
*:Zone.Identifier
