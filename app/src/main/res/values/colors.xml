<resources>
    <!--
      App icon background (issue #75). The foreground is now a raster PNG
      traced from docs/design/icon/icon.1.jpg (see
      app/src/main/res/drawable-*dpi/ic_launcher_foreground.png and
      docs/design/icon/README.md); its background was keyed out by border
      flood fill down to the artwork's own white, so this background layer
      is set to match it exactly, otherwise the foreground's square edge
      would show as a visible seam against a different background colour.
    -->
    <color name="ic_launcher_background">#FFFFFF</color>
</resources>
