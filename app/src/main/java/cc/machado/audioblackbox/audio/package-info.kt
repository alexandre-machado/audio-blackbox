/**
 * Audio capture and ring-buffer module.
 *
 * Placeholder package created by the project skeleton (issue #1).
 * Capture (AudioRecord) and in-memory ring buffer implementations land here.
 */
package cc.machado.audioblackbox.audio
